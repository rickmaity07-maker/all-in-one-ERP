{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Online updates and restart: desktop only (Android updates come as a new APK)",
  "platforms": ["windows", "linux", "macOS"],
  "windows": [
    "main"
  ],
  "permissions": [
    "updater:default",
    "process:allow-restart"
  ]
}
